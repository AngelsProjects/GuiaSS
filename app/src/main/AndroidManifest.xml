<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="moviles.guiass.ui">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize">

        </activity>
        <activity
            android:name=".InformationActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GeneralDataActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_datos_generales"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".WhereToDoActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_donde_realizar"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".StepByStepActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_proceso_pasoa_paso"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NextStepsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_pasos_siguientes"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Moresteps.ReportActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_reportes"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Moresteps.WorkPlanActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_plandetrabajo"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Moresteps.FinalStepActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_paso_final"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".interfaceActivity.TeacherInterface"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".interfaceActivity.GraphicInterfaceSecion"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".lateralmenu.MenuSliteActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_menu_slite"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".interfaceActivityStudent.graphic_student"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".interfaceActivity.AddStudent"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".serviceDocuments.SolicitudServicio"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".serviceDocuments.TarjetaControl"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".serviceDocuments.CartaAsignacion"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".serviceDocuments.CartaCompromiso"
            android:configChanges="orientation|screenSize"></activity>
    </application>

</manifest>