<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="moviles.guiass">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.InformationActivity" />
        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ui.GeneralDataActivity"
            android:label="@string/title_activity_datos_generales"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.WhereToDoActivity"
            android:label="@string/title_activity_donde_realizar"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.StepByStepActivity"
            android:label="@string/title_activity_proceso_pasoa_paso"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.NextStepsActivity"
            android:label="@string/title_activity_pasos_siguientes"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.Moresteps.ReportActivity"
            android:label="@string/title_activity_reportes"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.Moresteps.WorkPlanActivity"
            android:label="@string/title_activity_plandetrabajo"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.Moresteps.FinalStepActivity"
            android:label="@string/title_activity_paso_final"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.interfaceActivity.TeacherInterface"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.interfaceActivity.GraphicInterfaceSecion"></activity>
    </application>

</manifest>